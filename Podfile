source 'git@github.com:zawadz88/AnalyticsEventGeneratorSample-Specs.git'

target 'SampleiOSApp' do
    # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!
    platform :ios, '16.0'

    # Pods for SampleiOSApp
    # uncomment for local building (and comment the one below):
    # This assumes both projects share the same parent directory.
    # pod 'shared_analytics_library', :path => '../AnalyticsEventGeneratorSample-SharedLibrary'
    pod 'shared_analytics_library', '1.0.0'
end
